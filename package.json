{
  "name": "desktop_ai_app",
  "version": "1.0.0",
  "main": "electron/main.js",
  "scripts": {
    "dev": "npm-run-all --parallel dev:client dev:electron",
    "dev:client": "npm run dev --prefix client",
    "dev:electron": "wait-on http://localhost:5173 && electron ."
  },
  "devDependencies": {
    "electron": "^35.2.0",
    "npm-run-all": "^4.1.5",
    "wait-on": "^8.0.3"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "multer": "^1.4.5-lts.2",
    "openai": "^4.95.1"
  }
}
